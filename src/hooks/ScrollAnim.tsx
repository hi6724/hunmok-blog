import React from "react";

type Props = {
  children: React.ReactNode;
  timeline: GSAPTimeline;
};

function ScrollAnim({ children, timeline }: Props) {
  return <div>ScrollAnim</div>;
}

export default ScrollAnim;
